use std::env;
use clap::Arg;


fn main() {
    let args: Vec<String> = env::args().collect();

    println!("{:?}", args);

    println!("Length of args is {:?}", args.len());

    let graph = Vec::new();

    graph.contains(x)

}
