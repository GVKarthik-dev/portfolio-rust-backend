[package]
name = "weather"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "*", features = ["derive"]}
serde_json = "*"
reqwest = {version ="0.12.15", features = ["blocking", "json"]}
colored = "3.0.0"
dotenv = "0.15.0"